--Q1: select * from employees where address like '%Elgin,IL'

--Q2: select * from employees where b_date like '%197%'

----Q3:
--select * 
--from employees 
--where salary between 60000 and 70000
--and dep_ID = 5;
-- OR where salary >= 60000 and salary <= 70000\

--Q4A: 
--select * from employees
--order by dep_id

--4b
--select * from employees
--order by dep_id desc, l_name desc;

--Q5A:
--select count(*) as "Number of Employees", dep_id
--from employees
--group by dep_id

--Q5B
--select count(*) as "Number of Employees", dep_id, avg(salary)
--from employees
--group by dep_id;

--Q5C
--select count(*) as "NUM_EMPLOYEES", dep_id, avg(salary) as "AVG_SALARY"
--from employees
--group by dep_id

--Q5D
--select count(*) as "NUM_EMPLOYEES", dep_id, avg(salary) as "AVG_SALARY"
--from employees
--group by dep_id
--order by AVG_SALARY

--Q5E
-- //count(*) as "NUM_EMPLOYEES",
--select dep_id, avg(salary) as "AVG_SALARY"
--from employees
--group by dep_id
--having count(*) < 4
--order by AVG_SALARY

--Q6: 
--select employees.f_name, employees.l_name, departments.dep_name
--from employees, departments
--where employees.dep_id = departments.dept_id_dep 
--order by departments.dep_name desc, employees.l_name desc;







